package com.manav.ecommerce.miniapi.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
    // Relying on Spring Boot auto-configuration for Redis
}
